enum ContentFilterLevel {
  none,       // No filtering
  minimal,    // Filter only the most severe content
  moderate,   // Filter most sensitive content (default)
  strict      // Filter all potentially sensitive content
} 